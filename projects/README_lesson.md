# 📚 Урок: Создай свою первую игру на HTML и JavaScript!

## 🎯 Цель урока

Сегодня мы создадим простую игру **"Камень, ножницы, бумага"** на HTML и JavaScript, прямо в папке `projects`!

Мы научимся:
- ✅ Создавать HTML-файлы
- ✅ Использовать JavaScript для логики игры
- ✅ Работать с кнопками и событиями
- ✅ Выводить результаты на страницу
- ✅ Стилизовать игру с помощью CSS

---

## 📁 Шаг 1: Где создать файл?

### ⚠️ ВАЖНО! Правильный путь:

Создай файл **в папке `projects`**, которая находится в корне проекта.

### 📍 Полный путь будет такой:
```
portfolios_for_students/
  └── projects/
      └── rock-paper-scissors.html  ← ВОТ ЗДЕСЬ создай файл!
```

### 💡 Как найти папку `projects`:

1. Открой папку `portfolios_for_students`
2. Внутри найди папку `projects`
3. В этой папке создай новый файл `rock-paper-scissors.html`

**Проверь:** В папке `projects` уже должны быть файлы:
- `calculator.html`
- `guess-number.html`
- `tic-tac-toe.html`

Твой новый файл `rock-paper-scissors.html` должен быть **рядом** с ними!

---

## 🎮 Шаг 2: Что мы будем создавать?

Игра **"Камень, ножницы, бумага"**:
- Игрок нажимает кнопку: камень, ножницы или бумага
- Компьютер случайно выбирает свой вариант
- Программа определяет победителя и показывает результат!

---

## 📝 Шаг 3: Создаём файл

### Что делать:

1. Открой папку `projects` в любом редакторе кода (VS Code, PyCharm, или даже Блокнот)
2. Создай новый файл с именем: **`rock-paper-scissors.html`**
3. **ВАЖНО:** Расширение файла должно быть `.html` (не `.txt`, не `.py`!)

---

## 💻 Шаг 4: Начинаем писать код

### Часть 1: Базовая структура HTML

**Скопируй этот код в начало файла:**

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Камень, ножницы, бумага</title>
    <style>
        /* Здесь будет CSS для стилей */
    </style>
</head>
<body>
    <!-- Здесь будет содержимое страницы -->
</body>
</html>
```

**Что делает этот код:**
- `<!DOCTYPE html>` - говорит браузеру, что это HTML5
- `<html lang="ru">` - указывает язык страницы
- `<head>` - служебная информация для браузера
- `<body>` - видимое содержимое страницы
- `<style>` - место для CSS стилей

---

### Часть 2: Добавляем заголовок и контейнер

**Внутри `<body>` добавь:**

```html
<body>
    <div class="game-container">
        <h1>🎮 Камень, ножницы, бумага</h1>
        <p>Выбери свой вариант:</p>
        
        <!-- Здесь будут кнопки -->
        
        <!-- Здесь будет результат -->
    </div>
</body>
```

**Что делает этот код:**
- `<div class="game-container">` - контейнер для всей игры
- `<h1>` - главный заголовок
- `<p>` - обычный текст

---

### Часть 3: Добавляем кнопки

**Замени комментарий `<!-- Здесь будут кнопки -->` на:**

```html
<div class="buttons">
    <button id="rock" onclick="playGame('камень')">🪨 Камень</button>
    <button id="paper" onclick="playGame('бумага')">📄 Бумага</button>
    <button id="scissors" onclick="playGame('ножницы')">✂️ Ножницы</button>
</div>
```

**Что делает этот код:**
- `<button>` - создаёт кнопку
- `id="rock"` - уникальный идентификатор кнопки
- `onclick="playGame('камень')"` - при клике вызывает функцию `playGame` с параметром 'камень'
- `playGame()` - функция, которую мы создадим в JavaScript

---

### Часть 4: Добавляем место для результата

**Замени комментарий `<!-- Здесь будет результат -->` на:**

```html
<div id="result" class="result">
    <p>Выбери свой вариант, чтобы начать игру!</p>
</div>
```

**Что делает этот код:**
- `<div id="result">` - контейнер для результата игры
- `id="result"` - нужен, чтобы JavaScript мог найти этот элемент

---

### Часть 5: Добавляем JavaScript

**Перед закрывающим тегом `</body>` добавь:**

```html
<script>
    // Функция для игры
    function playGame(playerChoice) {
        // Варианты, которые может выбрать компьютер
        const options = ['камень', 'ножницы', 'бумага'];
        
        // Компьютер случайно выбирает свой вариант
        // Math.random() - случайное число от 0 до 1
        // Math.floor() - округляет вниз
        const randomIndex = Math.floor(Math.random() * 3);
        const computerChoice = options[randomIndex];
        
        // Находим элемент для вывода результата
        const resultDiv = document.getElementById('result');
        
        // Определяем победителя
        let message = '';
        
        if (playerChoice === computerChoice) {
            message = `🤝 Ничья! Вы оба выбрали ${playerChoice}!`;
        } else if (
            (playerChoice === 'камень' && computerChoice === 'ножницы') ||
            (playerChoice === 'ножницы' && computerChoice === 'бумага') ||
            (playerChoice === 'бумага' && computerChoice === 'камень')
        ) {
            message = `🎉 Ты выиграл! Ты выбрал ${playerChoice}, компьютер выбрал ${computerChoice}!`;
        } else {
            message = `😢 Ты проиграл! Ты выбрал ${playerChoice}, компьютер выбрал ${computerChoice}!`;
        }
        
        // Выводим результат на страницу
        resultDiv.innerHTML = `
            <h2>Результат:</h2>
            <p>Твой выбор: <strong>${playerChoice}</strong></p>
            <p>Выбор компьютера: <strong>${computerChoice}</strong></p>
            <p class="message">${message}</p>
        `;
    }
</script>
```

**Что делает этот код:**
- `function playGame(playerChoice)` - создаёт функцию с параметром
- `const options = [...]` - список вариантов
- `Math.random() * 3` - случайное число от 0 до 3
- `Math.floor()` - округляет вниз
- `document.getElementById('result')` - находит элемент по id
- `resultDiv.innerHTML = ...` - вставляет HTML в элемент
- `if/else if/else` - проверяет условия

---

### Часть 6: Добавляем стили CSS

**Внутри `<style>` добавь:**

```css
body {
    font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    margin: 0;
    padding: 20px;
    display: flex;
    justify-content: center;
    align-items: center;
}

.game-container {
    background: white;
    border-radius: 20px;
    padding: 40px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    max-width: 600px;
    text-align: center;
}

h1 {
    color: #333;
    margin-bottom: 20px;
}

.buttons {
    display: flex;
    gap: 15px;
    justify-content: center;
    margin: 30px 0;
    flex-wrap: wrap;
}

.buttons button {
    padding: 15px 30px;
    font-size: 1.2rem;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.3s;
    font-weight: bold;
}

#rock {
    background: #8B4513;
    color: white;
}

#paper {
    background: #4169E1;
    color: white;
}

#scissors {
    background: #FF6347;
    color: white;
}

.buttons button:hover {
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

.result {
    margin-top: 30px;
    padding: 20px;
    background: #f8f9fa;
    border-radius: 10px;
}

.result h2 {
    color: #333;
    margin-top: 0;
}

.message {
    font-size: 1.3rem;
    font-weight: bold;
    margin-top: 15px;
    padding: 15px;
    border-radius: 8px;
    background: white;
}
```

**Что делает этот код:**
- `background: linear-gradient(...)` - градиентный фон
- `border-radius: 20px` - скруглённые углы
- `padding: 40px` - отступы внутри
- `cursor: pointer` - курсор-указатель при наведении
- `transition: all 0.3s` - плавная анимация
- `transform: translateY(-3px)` - поднимает кнопку при наведении

---

## 🎯 Шаг 5: Полный код (для проверки)

Вот как должен выглядеть **полный файл** `rock-paper-scissors.html`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Камень, ножницы, бумага</title>
    <style>
        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .game-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 600px;
            text-align: center;
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
        }

        .buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin: 30px 0;
            flex-wrap: wrap;
        }

        .buttons button {
            padding: 15px 30px;
            font-size: 1.2rem;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
        }

        #rock {
            background: #8B4513;
            color: white;
        }

        #paper {
            background: #4169E1;
            color: white;
        }

        #scissors {
            background: #FF6347;
            color: white;
        }

        .buttons button:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .result {
            margin-top: 30px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
        }

        .result h2 {
            color: #333;
            margin-top: 0;
        }

        .message {
            font-size: 1.3rem;
            font-weight: bold;
            margin-top: 15px;
            padding: 15px;
            border-radius: 8px;
            background: white;
        }
    </style>
</head>
<body>
    <div class="game-container">
        <h1>🎮 Камень, ножницы, бумага</h1>
        <p>Выбери свой вариант:</p>
        
        <div class="buttons">
            <button id="rock" onclick="playGame('камень')">🪨 Камень</button>
            <button id="paper" onclick="playGame('бумага')">📄 Бумага</button>
            <button id="scissors" onclick="playGame('ножницы')">✂️ Ножницы</button>
        </div>
        
        <div id="result" class="result">
            <p>Выбери свой вариант, чтобы начать игру!</p>
        </div>
    </div>

    <script>
        function playGame(playerChoice) {
            const options = ['камень', 'ножницы', 'бумага'];
            const randomIndex = Math.floor(Math.random() * 3);
            const computerChoice = options[randomIndex];
            
            const resultDiv = document.getElementById('result');
            
            let message = '';
            
            if (playerChoice === computerChoice) {
                message = `🤝 Ничья! Вы оба выбрали ${playerChoice}!`;
            } else if (
                (playerChoice === 'камень' && computerChoice === 'ножницы') ||
                (playerChoice === 'ножницы' && computerChoice === 'бумага') ||
                (playerChoice === 'бумага' && computerChoice === 'камень')
            ) {
                message = `🎉 Ты выиграл! Ты выбрал ${playerChoice}, компьютер выбрал ${computerChoice}!`;
            } else {
                message = `😢 Ты проиграл! Ты выбрал ${playerChoice}, компьютер выбрал ${computerChoice}!`;
            }
            
            resultDiv.innerHTML = `
                <h2>Результат:</h2>
                <p>Твой выбор: <strong>${playerChoice}</strong></p>
                <p>Выбор компьютера: <strong>${computerChoice}</strong></p>
                <p class="message">${message}</p>
            `;
        }
    </script>
</body>
</html>
```

---

## 🚀 Шаг 6: Как открыть игру?

### Способ 1: Двойной клик

1. Найди файл `rock-paper-scissors.html` в папке `projects`
2. Дважды кликни по нему
3. Игра откроется в браузере!

### Способ 2: Через браузер

1. Открой браузер (Chrome, Firefox, Edge)
2. Нажми `Ctrl+O` (или `Cmd+O` на Mac)
3. Найди файл `rock-paper-scissors.html`
4. Открой его

### Способ 3: Перетащить файл

1. Открой браузер
2. Перетащи файл `rock-paper-scissors.html` в окно браузера

---

## ✅ Шаг 7: Проверь, что всё работает!

1. Открой игру в браузере
2. Нажми на кнопку "🪨 Камень"
3. Посмотри результат
4. Попробуй другие кнопки

---

## 🎓 Что мы изучили?

- ✅ **HTML** - структура страницы (`<div>`, `<button>`, `<h1>`)
- ✅ **JavaScript** - логика игры (`function`, `if/else`, `Math.random()`)
- ✅ **CSS** - стили и оформление (`background`, `border-radius`, `padding`)
- ✅ **События** - `onclick` для реакции на клик
- ✅ **DOM** - `document.getElementById()` для работы с элементами
- ✅ **Работа с путями** - где создавать файлы

---

## 🎯 Дополнительные задания (для продвинутых)

Попробуй улучшить игру:

1. **Добавь счётчик побед:**
   ```javascript
   let wins = 0;
   let losses = 0;
   // Увеличивай счётчик при победе/проигрыше
   ```

2. **Добавь кнопку "Новая игра":**
   ```html
   <button onclick="resetGame()">Новая игра</button>
   ```

3. **Добавь анимацию:**
   ```css
   @keyframes bounce {
       0%, 100% { transform: translateY(0); }
       50% { transform: translateY(-10px); }
   }
   ```

4. **Измени цвета кнопок:**
   - Найди `#8B4513`, `#4169E1`, `#FF6347` в CSS
   - Замени на свои любимые цвета!

---

## 📍 Важно помнить о путях!

### ✅ Правильно:
```
portfolios_for_students/
  └── projects/
      └── rock-paper-scissors.html  ← ТВОЙ ФАЙЛ ЗДЕСЬ!
```

### ❌ Неправильно:
```
portfolios_for_students/
  └── projects on python/  ← НЕ ЗДЕСЬ!
      └── rock-paper-scissors.html
```

**Помни:** Файл должен быть в папке `projects`, а не в `projects on python`!

---

## 🆘 Если что-то не работает

1. **Проверь путь к файлу** - файл должен быть в папке `projects`
2. **Проверь расширение** - файл должен называться `rock-paper-scissors.html` (с `.html`)
3. **Проверь консоль браузера:**
   - Нажми `F12` в браузере
   - Открой вкладку "Console"
   - Посмотри, есть ли ошибки (они будут красным цветом)
4. **Проверь кавычки** - используй обычные кавычки `"` или `'`
5. **Проверь закрывающие теги** - каждый `<div>` должен иметь `</div>`

---

## 💡 Полезные советы

1. **Используй отступы** - это делает код читаемым
2. **Комментируй код** - `// это комментарий` помогает понять, что делает код
3. **Проверяй в консоли** - `console.log()` выводит информацию для отладки
4. **Экспериментируй** - меняй цвета, размеры, текст и смотри, что получается!

---

## 🎉 Поздравляю!

Ты создал свою первую игру на HTML и JavaScript! 

Теперь ты умеешь:
- Создавать HTML-файлы
- Использовать JavaScript для логики
- Стилизовать страницы с помощью CSS
- Работать с событиями и кнопками
- Работать с путями к файлам

**Продолжай учиться и создавай новые игры!** 🚀

---

## 📚 Сравнение с другими проектами

Посмотри на другие проекты в папке `projects`:
- `calculator.html` - калькулятор
- `guess-number.html` - угадай число
- `tic-tac-toe.html` - крестики-нолики

Твоя игра теперь в том же стиле! Можешь использовать их как примеры для новых идей!
